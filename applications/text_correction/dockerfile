# 从同一个基础镜像开�?
FROM base-agent:v1

WORKDIR /app

# 暴露端口
EXPOSE 8007

# 启动命令
CMD ["sh", "-c", "uvicorn applications.text_correction.text_correction_app:app --host 0.0.0.0 --port ${IE_SERVICE_PORT:-8007} --workers ${IE_SERVICE_WORKERS:-1}"]
