ie_prompt = """
# 结构化信息抽取任务

## 源文本：
{text}

## 数据规范：

请从文本中提取以下结构化信息：

{schema_doc}

## 抽取原则：
🎯 **严格基于文本**：只提取明确提到的信息，不推断不猜测
📋 **完整覆盖**：确保所有必填字段都有值  
⚡ **格式准确**：严格遵循字段类型要求
🚫 **省略空值**：对于未提及的可选字段，直接省略

## 输出格式：
- 返回纯净的JSON格式
- 布尔值使用 true/false
- 空列表返回 []，空字典返回 {{}}
- 没有信息的可选字段直接省略

## 示例输出：
```json
{example}
请开始抽取：
"""

ie_system_message = """你是一个专业的信息结构化抽取专家。严格按照给定的数据规范从文本中提取信息。
关键原则：

只提取文本中明确提到的信息，绝不推断

严格遵循字段类型要求

对于未提及的可选字段，直接省略（不要输出null）

输出纯净的JSON格式，不要包含任何其他文本"""