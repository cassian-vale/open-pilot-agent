# 从同一个基础镜像开�?
FROM base-agent:v1

WORKDIR /app

# 暴露端口
EXPOSE 8001

# 启动命令
CMD ["sh", "-c", "uvicorn applications.information_extraction.information_extract_app:app --host 0.0.0.0 --port ${IE_SERVICE_PORT:-8001} --workers ${IE_SERVICE_WORKERS:-1}"]
