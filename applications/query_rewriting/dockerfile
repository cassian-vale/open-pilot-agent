# 从同一个基础镜像开�?
FROM base-agent:v1

WORKDIR /app

# 暴露端口
EXPOSE 8003

# 启动命令
CMD ["sh", "-c", "uvicorn applications.query_rewriting.query_rewriting_app:app --host 0.0.0.0 --port ${IE_SERVICE_PORT:-8003} --workers ${IE_SERVICE_WORKERS:-1}"]
